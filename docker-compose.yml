version: '3.8'

services:
  postgres:
    container_name: nest-teste-wa-pg
    image: postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nest-teste-wa
      PGDATA: /data/postgres